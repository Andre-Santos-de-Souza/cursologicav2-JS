//Dado o array abaixo, use forEach para imprimir no console o dobro de cada número.

const numeros = [2, 4, 6, 8, 10];

const n = numeros.forEach(function(elemento, indice, array){
    console.log(elemento*2)
})

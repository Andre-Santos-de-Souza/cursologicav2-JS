// Crie um array com 5 números e use um for para multiplicar cada número por 2 e exibir o resultado.

let array = [1,2,3,4,5]
let n = 2
let mult = 0
for(let i = 0; i < array.length;i++){
    mult = n*array[i]
    console.log(mult)
}